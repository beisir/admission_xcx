<view class='mapBox'>
    <map class="map"
        longitude="{{lng}}"
        latitude="{{lat}}"
        markers="{{markers}}"
    ></map>
</view>
<view class='mapBtn'>
    <cover-view class="btn-map" bindtap="navigateStore">
        <cover-view >导航</cover-view >
        <cover-view >到店</cover-view >
    </cover-view>
    <!-- <button type='button' >导航到店</button> -->
</view>